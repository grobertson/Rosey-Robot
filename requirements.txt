# Core dependencies for CyTube bot library
websockets>=12.0
requests>=2.32.3
aiohttp>=3.9.0  # For async HTTP requests (LLM providers)
aiosqlite>=0.19.0  # Async SQLite wrapper for BotDatabase

# NATS messaging (for Quicksilver architecture)
nats-py>=2.7.0  # NATS client for event-driven messaging

# Plugin system
watchdog>=3.0.0  # For hot reload file watching
packaging>=23.0  # For semantic versioning in service registry

# LLM providers (optional but recommended)
openai>=1.0.0   # For OpenAI API and compatible endpoints (Azure, LocalAI, LM Studio)

# Web status server
Flask>=3.0.0

# Optional dependencies
PySocks>=1.7.1  # For proxy support
PyYAML>=6.0    # For YAML config files (optional, JSON still works)

# Bot-specific dependencies
markovify>=0.9.4  # For markov bot
blessed>=1.20.0   # For TUI bot

# Testing dependencies
pytest>=8.2.2
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
pytest-timeout>=2.3.0
pytest-json-report>=1.5.0  # For benchmark reporting (Sprint 10 Sortie 4)
freezegun>=1.5.0  # For mocking time
psutil>=5.9.0  # For performance benchmarking
